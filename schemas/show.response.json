{
  "type": "object",
  "required": [
    "schema_version",
    "operation",
    "repository",
    "index",
    "result",
    "truncated"
  ],
  "properties": {
    "schema_version": {
      "description": "Response contract version.",
      "type": "integer",
      "const": 1
    },
    "operation": {
      "description": "Always \"show\".",
      "type": "string",
      "const": "show"
    },
    "repository": {
      "description": "Canonical path of the routed repository.",
      "type": "string"
    },
    "index": {
      "description": "The published code-graph index that answered.",
      "type": "object",
      "required": [
        "revision",
        "checkout_revision",
        "fresh",
        "worktree_dirty",
        "synced_at",
        "files"
      ],
      "properties": {
        "revision": {
          "description": "Commit the index was built at; null on an unborn branch.",
          "type": [
            "string",
            "null"
          ]
        },
        "checkout_revision": {
          "description": "Current checkout commit; null when HEAD is unborn or unreadable.",
          "type": [
            "string",
            "null"
          ]
        },
        "fresh": {
          "description": "True when the index was built at checkout_revision. When false, results describe older code and stale names the fix.",
          "type": "boolean"
        },
        "worktree_dirty": {
          "description": "Whether uncommitted changes were indexed along with revision.",
          "type": "boolean"
        },
        "synced_at": {
          "description": "When the index was published (RFC 3339, UTC).",
          "type": "string"
        },
        "files": {
          "description": "Files in the index.",
          "type": "integer"
        },
        "stale": {
          "description": "Present when fresh is false: why, and the call that refreshes the index.",
          "type": "object",
          "required": [
            "reason",
            "fix"
          ],
          "properties": {
            "reason": {
              "description": "Which revisions differ.",
              "type": "string"
            },
            "fix": {
              "description": "The maintenance call to run, then retry the query.",
              "type": "object",
              "required": [
                "tool",
                "input"
              ],
              "properties": {
                "tool": {
                  "description": "Tool to call: orbit.graph.maintain, or graph.maintain for a non-first-party install.",
                  "type": "string"
                },
                "input": {
                  "description": "Its input: {\"operation\":\"graph_sync\"}.",
                  "type": "object"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "result": {
      "description": "The orbit-graph library result, passed through unshaped (it may gain fields): NodeView: metadata and source bytes; null when the selector names nothing in the index.",
      "type": [
        "object",
        "null"
      ]
    },
    "truncated": {
      "description": "Always false: max_bytes bounds show, and metadata.truncated reports a source cut.",
      "type": "boolean"
    },
    "truncation": {
      "description": "Never present for show.",
      "type": "object"
    }
  },
  "additionalProperties": false
}
