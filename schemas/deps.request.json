{
  "type": "object",
  "required": [
    "selector"
  ],
  "properties": {
    "schema_version": {
      "description": "Request contract version; must be 1 when present.",
      "type": "integer",
      "const": 1
    },
    "repository": {
      "description": "Absolute path of the Git repository to query. Defaults to the caller's workspace root; never inferred from the tool cwd.",
      "type": "string",
      "minLength": 1
    },
    "selector": {
      "description": "File or directory selector: file:<path> or dir:<path>.",
      "type": "string",
      "minLength": 1
    },
    "limit": {
      "description": "Cap on each top-level result array (imports); 1-500, default 50. Cut arrays are reported in truncation.",
      "type": "integer",
      "minimum": 1,
      "maximum": 500,
      "default": 50
    }
  },
  "additionalProperties": false
}
