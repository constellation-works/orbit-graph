{
  "type": "object",
  "properties": {
    "schema_version": {
      "description": "Request contract version; must be 1 when present.",
      "type": "integer",
      "const": 1
    },
    "repository": {
      "description": "Absolute path of the Git repository to inspect. Defaults to the caller's workspace root; never inferred from the tool cwd.",
      "type": "string",
      "minLength": 1
    },
    "branch": {
      "description": "Landing branch whose history scope is reported.",
      "type": "string",
      "minLength": 1,
      "default": "main"
    }
  },
  "additionalProperties": false
}
